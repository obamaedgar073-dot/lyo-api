[build]
command = "npm ci && npx prisma generate && npx prisma migrate deploy && npx tsc -p tsconfig.json --noEmitOnError false || true"

[deploy]
startCommand = "node -r tsconfig-paths/register dist/server.js"
healthcheckPath = "/health"
healthcheckTimeout = 30